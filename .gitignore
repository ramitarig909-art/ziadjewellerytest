# OS / editor
.DS_Store
Thumbs.db
*.log

# Dependencies (none required — this is a static site)
node_modules/

# Secrets — NEVER commit the Supabase service_role key or any .env file.
.env
.env.*

# Keep assets/js/lib/config.js committed: it only holds the PUBLIC anon key,
# which is safe to expose (Row Level Security protects your data).
